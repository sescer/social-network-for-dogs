spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:13.2:///snfd-postgres
  main:
    lazy-initialization: true

jwt:
  token:
    secret: ${TOKEN_SECRET:socialNetworkForDogsAppDemo}
    expired: ${TOKEN_EXPIRED:1}
